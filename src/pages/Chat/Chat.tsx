import { Button } from 'antd';
import React from 'react';
import './Chat.scss';

const Chat = () => (
  <section>
    <h1>Chat page</h1>
    <Button> Hello world </Button>
  </section>
);

export default Chat;
